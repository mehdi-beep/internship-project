/**
 * PROTOTYPE / ILLUSTRATIVE ONLY — NOT REAL, EXECUTABLE CODE.
 *
 * See ../../ISOLATION.md — this file:
 *   - uses a .tsx.example extension so Vite/TypeScript cannot compile or bundle it
 *   - is NOT imported anywhere in frontend/src/
 *   - is NOT rendered by InterventionReviewViewer.tsx or any other real component
 *
 * This illustrates the PROPOSED shape of the optional "Get AI Suggestion" panel
 * described in SPECIFICATION.md Section 11 (Integration Plan, step 1) — the one
 * and only touch point this future module would ever have with the real,
 * existing application, and even that is explicitly NOT implemented yet.
 */

// Deliberately illustrative pseudo-code, not a real, compilable component —
// imports below reference real project conventions for readability, but this
// file is never built or type-checked as part of the real application.

interface AISuggestionPanelProps {
  interventionId: number;
  /** Only rendered for chef_technicien / admin_supervisor reviewers — see
   * SPECIFICATION.md Section 11, step 1. */
  visible: boolean;
}

/**
 * PROPOSED shape only. If ever built for real, this would:
 *   1. Call the SEPARATE ai-review service's API directly (never through the
 *      real BIMS backend — see SPECIFICATION.md Section 3's architecture
 *      diagram: "future, optional HTTPS call to a SEPARATE service, never a
 *      direct import").
 *   2. Show a loading state while the (likely slow) analysis job runs.
 *   3. Render the per-field explanation list (SPECIFICATION.md Section 9) and
 *      the suggested_decision as a clearly-labeled, non-authoritative
 *      SUGGESTION — never as something that submits an Approve/Reject
 *      decision on the reviewer's behalf.
 *   4. Leave the existing Approve/Reject buttons in
 *      InterventionReviewViewer.tsx completely untouched — this panel is
 *      purely additive, informational context alongside them.
 */
export default function AISuggestionPanel(_props: AISuggestionPanelProps) {
  throw new Error(
    "Prototype only — not a real component. See SPECIFICATION.md Section 11.",
  );
}
